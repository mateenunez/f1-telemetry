name: Deploy to VPS

on:
  push:
    branches: [ master ]
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and Push
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_API="${{ secrets.NEXT_PUBLIC_API }}" \
            --build-arg NEXT_PUBLIC_WS="${{ secrets.NEXT_PUBLIC_WS }}" \
            --build-arg NEXT_PUBLIC_LINKEDIN_URL="${{ secrets.NEXT_PUBLIC_LINKEDIN_URL }}" \
            --build-arg NEXT_PUBLIC_GITHUB_URL="${{ secrets.NEXT_PUBLIC_GITHUB_URL }}" \
            --build-arg NEXT_PUBLIC_DISCORD_INVITE_URL="${{ secrets.NEXT_PUBLIC_DISCORD_INVITE_URL }}" \
            -t ${{ secrets.DOCKER_USERNAME }}/f1t-frontend:latest .
          
          docker push ${{ secrets.DOCKER_USERNAME }}/f1t-frontend:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Execute remote ssh commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /root/f1t-dock
            docker-compose pull web-service
            docker-compose up -d
